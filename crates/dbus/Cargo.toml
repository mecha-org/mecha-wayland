[package]
name = "dbus"
version = "0.1.0"
edition = "2024"

[dependencies]
app = { path = "../app" }
io-ring = { path = "../io-ring" }
io-uring = { workspace = true }
libc = { workspace = true }
serde.workspace = true
zbus = { version = "5.16.0", features=["bus-impl"] }

